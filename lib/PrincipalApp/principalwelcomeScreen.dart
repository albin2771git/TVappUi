import 'package:flutter/material.dart';

class PrincipalWelcomeScreen extends StatelessWidget {
  const PrincipalWelcomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "done",style: TextStyle(
          color: Colors.black
        ),
        ),
      ),
    );
  }
}
